#
pushd ..
#
export gitSource=https://github.com/fastice
#
for repo in $(echo clib cRecipes fft landsatSource64 rdfSource speckleSource triangle unwrapSource utilsSource fft)
do
        if ! -e $repo
        then 
           echo cloning $repo
           git clone $gitSource/$repo
        fi
done

